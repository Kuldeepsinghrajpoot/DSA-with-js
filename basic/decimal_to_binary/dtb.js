function decimalToBinary(decimal) {
    return decimal.toString(2);
}

const decimal = 2;
const binary = decimalToBinary(decimal);
console.log(binary); // Output: 101010


// here using the toString for converting the digital to binary number
